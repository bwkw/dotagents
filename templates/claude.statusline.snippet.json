{
  "$comment": [
    "Merged only when `setup.sh install --statusline` is passed. A status line is a visible",
    "personal choice, so it is opt-in rather than part of the default install.",
    "Recorded in the manifest like every other key, so `uninstall` takes it back."
  ],
  "statusLine": {
    "type": "command",
    "command": "$HOME/.claude/hooks/dotagents-statusline.sh"
  }
}
