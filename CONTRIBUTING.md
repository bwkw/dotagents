# Contributing

これは個人用のツールキットです。**あなたの開発ループに合わせるのが正しい使い方**なので、fork して自分のものにするのを歓迎します。upstream への PR も歓迎しますが、まず知っておいてほしいことが2つあります。

## 1. ここに入っているのは、自動実行されるコードです

`scripts/setup.sh install` は hook を2つ配線します。片方は**すべての `Write` / `Edit`** で、もう片方は**すべてのターン終了時**に走ります。そしてスキルは symlink なので、**この checkout の中身がそのまま、エージェントが従う命令**です。`git pull` した内容は、次にスキルが読まれた瞬間から有効になります。

つまり `hooks/` と `skills/**/SKILL.md` の変更は、レビュアのマシンで自動実行されるものの変更です。詳しくは [SECURITY.md](SECURITY.md)。

## 2. 失敗の向きを、意識して選んでください

このリポジトリの中心的な規約です。

| コンポーネント | 失敗の向き | 理由 |
|---|---|---|
| `dotagents-lint-skill-frontmatter.sh` | **fail open** | 検査するだけ。壊れたときに普通の編集を止めてはいけない |
| `dotagents-verify-gate.sh` | **fail closed** | 開くガードレールは、無いより悪い |

どちらの性質に触る変更でも、**どちらを選んだか**と**その理由**を PR に書いてください。「動くようにした」だけでは足りません。ハーネス側の実挙動（exit code 2 以外は全部通る、timeout は non-blocking など）は [docs/harness-facts.md](docs/harness-facts.md) にあります。

## やること

```bash
./scripts/check.sh        # 全部。これが通らないものは見ません
```

`check.sh` はローカルと CI で同じものを回します（CI 専用は、Python パッケージが要る YAML パースと profile スキーマ検証の2つだけ）。**PR には出力を貼ってください。** 「通りました」は検証の報告ではありません。

- 新しい挙動には、**それが壊れたら落ちるテスト**を付けてください。テストのないガードレールは、ガードレールの見た目です
- `AGENTS.md` の不変条件を読んでください。破るなら、破る理由を書いてください
- 決定を変えるときは [docs/decisions.md](docs/decisions.md) に追記してください。**撤回した判断も残します** —— このリポジトリで一番読む価値があるのはそこです

## 書き方の作法

コメントは「何をしているか」ではなく**「なぜこの形なのか」**を書きます。多くのコメントが、実際に踏んだ失敗の記録です。それを消すと理由が消えるので、リファクタで消さないでください。

一方で、**特定の環境の固有名は入れないでください** —— 絶対パス、ユーザ名、雇用主のリポジトリ名。観測の記録は残し、固有名だけ外すのが正しい形です。検出手順は [docs/portability.md](docs/portability.md) にあります。

## 何を受け付けないか

- **`profiles/*.json`**（`_example.*` と `dotagents.json` を除く）。あれは個人設定で、実在のリポジトリ名と、ときに雇用主の内部規則を含みます。`.gitignore` は allowlist なので、新しい profile は opt-in しない限り追跡されません
- **上流スキルの vendoring。** 方法論はそれを本業にしている人たちが維持した方が良いので、コピーせず横に並べます。理由は README にあります
- **未確認の事実を仕様として書くこと。** 「たぶんこう動く」は `docs/harness-facts.md` の未確認リストに書いてください。そこに書くのは、次に読む人が「抜け」と読まないためです

## 脆弱性

issue ではなく [Security Advisory](https://github.com/bwkw/dotagents/security/advisories/new) へ。[SECURITY.md](SECURITY.md) を読んでください。
